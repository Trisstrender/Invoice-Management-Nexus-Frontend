/**
 * Enum for countries supported in the application.
 * @readonly
 * @enum {string}
 */
const Country = Object.freeze({
    CZECHIA: 'CZECHIA',
    SLOVAKIA: 'SLOVAKIA',
});

export default Country;